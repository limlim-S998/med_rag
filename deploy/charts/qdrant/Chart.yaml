apiVersion: v2
name: qdrant
version: 0.3.1
appVersion: "1.19.0"
# No medw-lib dependency: this is the one workload that is a StatefulSet, not
# a Deployment, so it shares none of the library templates. A chart that had
# to be conditional about which kind of workload it renders would be the point
# at which Helm stopped being thin.
