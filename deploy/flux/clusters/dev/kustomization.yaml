apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system
  - platform-config.yaml
  - reconciliation.yaml
# flux bootstrap maintains ./flux-system. The cluster never consumes
# operator-private Terraform state or credentials from the source repository.
