apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
# The secrets initialization command adds secrets.sops.yaml after encryption.
# A fresh cluster stays unready until credentials have been initialized.
