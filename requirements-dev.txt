# Host development: make dev, or install this file into an activated venv.
# The editable shared library plus all five service input requirements make
# the retained platform and test suites importable. Docker installs each
# service's separate exact/hash-checked requirements.txt, not this host union.
-e ./libs/medw_core[dev]
-r services/retrieval/requirements.in
-r services/gateway/requirements.in
-r services/generation/requirements.in
-r services/ingestion_worker/requirements.in
-r services/reranker/requirements.in

# The installed lexical placeholder needs neither torch nor downloaded weights.
# Replacing it with a learned reranker requires new dependency and model locks.
# Airflow remains an optional scheduler environment: install its exact release
# against its own official Python/version constraints, not into this host union.
import-linter
pytest-asyncio
pyyaml
opentelemetry-exporter-prometheus
prometheus-client
